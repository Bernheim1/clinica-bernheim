import { BotonHistoriaClinicaDirective } from './boton-historia-clinica.directive';

describe('BotonHistoriaClinicaDirective', () => {
  it('should create an instance', () => {
    const directive = new BotonHistoriaClinicaDirective();
    expect(directive).toBeTruthy();
  });
});
