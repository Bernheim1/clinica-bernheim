import { BotonBienvenidaDirective } from './boton-bienvenida.directive';

describe('BotonBienvenidaDirective', () => {
  it('should create an instance', () => {
    const directive = new BotonBienvenidaDirective();
    expect(directive).toBeTruthy();
  });
});
